# meal-tracker-rob-ot
A super tailored meal tracker app built entirely around my food needs
Data is stored in localStorage, which is per browser per device.
V2 (Tue 23rd): Possibilita' in setting di aggiungere nuovi food item manualmente (con opzione per unità o per 100g) al database / Messe in evidenza quando i pesi si riferiscono a unità mettendo (u) di fianco al nome del cibo
aggiunto un logo / eliminati gli errori accidentali nella pagina TODAY, facendo collassare il log giornaliero sotto a scomparsa, in modo tale che di default non sia visibile / La barra dello streak viene ora in piccolo counter in alto a destra / per considerare una giornata in target per lo streak, considerare un +/- 200 kcal dal valore impostato / di default, tutte le volte che si apre ADD, il database alimenti è tutto chiuso, q / la AI FOOD SEARCH viene eliminata
